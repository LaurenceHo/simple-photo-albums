{
  "name": "simple-photo-albums",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["ui", "server"],
  "scripts": {
    "lint": "bun run --cwd ui lint && bun run --cwd server lint",
    "test:all": "bun run --cwd ui test:unit && bun run --cwd server test:server",
    "type-check": "bun run --cwd ui type-check && bun run --cwd server types:server",
    "dev": "bun run --cwd ui dev",
    "build": "bun run --cwd ui build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "engines": {
    "node": "^22",
    "npm": ">= 10"
  }
}
